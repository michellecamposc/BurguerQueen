export const HomePage = () => {
  return <h1>Deja Burg</h1>;
};

export const Button = ({ title }) => {
  <>
    return
    <button className="mainButton" id={title.toLowerCase()}>
      {title}
    </button>
  </>;
};
